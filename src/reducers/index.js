import { reducer } from 'redux'
import {
  ADD_POST,
  DELETE_POST
} from '../actions'

function posts (action){
  const {title, body, author, category}

  switch (action.type)
  {
    case ADD_POST:
      return {
      }
    case DELETE_POST:
      return {

      }
    default:
      return state
  }
}
